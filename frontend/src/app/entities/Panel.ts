export interface Panel {
  amount: number,
  amountUnit: string,
  breakEvenPoint: number,
  co2saved: number[],
  production: number[],
  revenue: number[],
  savings: number[],
  type: string,
  subType: string
}
